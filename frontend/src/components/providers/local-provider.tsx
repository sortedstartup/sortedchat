import { ProviderView } from './provider-view';

export const LocalProvider = () => {
    return (
        <ProviderView
            providerName="local"
            displayName="Local (llama.cpp)"
        />
    );
};
